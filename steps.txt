development
---------------
1. start virtual environment - venv\Scripts\activate
2. running backend - uvicorn main:app --host 0.0.0.0 --port 8000 --reload

    we can view docs - http://localhost:8000/docs

    testing (simulate esp32) - curl.exe --% -X POST "https://smartstudy-backend-psi.vercel.app/api/sensor/event" -H "Content-Type: application/json" -d "{\"area id\": \"LIBRARY_01\", \"event\": \"ENTER\"}"
3. hardware changes needed
    // Wi-Fi credentials and your computer's Local IP
    const char* ssid = "YOUR_WIFI_SSID";
    const char* password = "YOUR_WIFI_PASSWORD";
    
    // The live URL provided by Render or your hosting platform
    const char* serverUrl = "https://your-app-name.onrender.com/api/sensor/event";

4. Find Hotspot SSID
    Open your phone's settings and navigate to Mobile Hotspot (Android) or Personal Hotspot (iOS). 
    Note down the Network Name (SSID) and the Password. 
    You can temporarily rename the SSID to something simple without spaces (like SmartStudyNet) to prevent typos in the ESP32 code.

5. Connect Laptop and Find IPv4
    Connect Laptop and Find IPv4:Windows.Connect your laptop to this mobile hotspot. 
    Open the Command Prompt and type ipconfig.
     Scroll to the section labeled Wireless LAN adapter Wi-Fi and note the IPv4 Address (it will likely start with 192.168. or 172.20.).

# important
To verify this setup is working before the ESP32 is even connected, ensure your FastAPI app is running on 0.0.0.0.
 Then, open a web browser on your phone (which is broadcasting the hotspot) and type http://YOUR_IPV4:8000/api/study-areas.



26/09/20
===========
vercel hosting 
-----------------
# github repo connected .
# pip install -r requirements.txt this required when installing libraries
# building command not required
# link - https://smartstudy-backend-psi.vercel.app/api/
# since web api connected to github repo , changes will sync 
# Vercel hosting has no sleeping after inactivity - so uptime monitor requesting/ or hardware auto request not needed

    Cold Start Duration: While Render's free tier can cause a 10–30 second timeout after 15 minutes of inactivity, Vercel's cold starts happen in a fraction of a second to a few seconds (depending on the weight of your Python imports and libraries).

    No Heartbeat Required: You do not need to configure cron jobs or uptime monitors to keep a Vercel deployment from timing out or suspending.